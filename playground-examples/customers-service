query get_customers {
  customers {
    id
    name
    address
    license_id
  }
}

mutation create_customers {
  createCustomer(
    input: {name: "near nguyen", status: "active", address: "fake address", license_id: "00000000000000000", phone_number: "0123456789"}
  ) {
    name
  }
}

# update_customers
# delete_customers
# set_status

! email validation for user and customer
! search