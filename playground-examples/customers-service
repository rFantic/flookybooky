query get_customers {
  customers {
    id
    name
    address
    license_id
  }
}

mutation create_customers {
  createCustomer(
    input: {name: "near nguyen", address: "fake address", license_id: "034098021748", phone_number: "0123456789"}
  ) {
    name
  }
}

# update_customers
# delete_customers
# set_status

! email validation for user and customer
! search
